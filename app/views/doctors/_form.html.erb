<%= form_with(model: doctor, local: true) do |form| %>
  <% if doctor.errors.any? %>
    <div id="error_explanation">
      <h2 class="alert alert-danger"><%= pluralize(doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
        <% doctor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group" action="/login">

    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, :class => 'form-control' %>
    </div>

    <div class="field">
      <%= form.label :position %>
      <%= form.text_field :position, :class => 'form-control' %>
    </div>

    <div class="field">
      <%= form.label :img %>
      <%= form.text_field :img, :class => 'form-control' %>
    </div>

    <div class="actions">
      <%= form.submit :class => 'btn btn-outline-primary' %>
    </div>
  </div>
<% end %>
