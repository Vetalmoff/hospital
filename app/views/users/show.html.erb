<% if notice %>
  <p id="notice", class="alert alert-success"><%= notice %></p>
<% end %>
<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user), :class => 'btn btn-outline-secondary mt-3' %> |
<%= link_to 'Back', users_path, :class => 'btn btn-outline-secondary mt-3' %>
